draw_self();

//if obj_Control.currentGameState == gameState.gameOver return; // Gjør ikke koden under, dersom "Game Over" er sant.

scr_showTargettingCircle();

//draw_text(100, 140, "Shoot Counter: " + string(counter));
