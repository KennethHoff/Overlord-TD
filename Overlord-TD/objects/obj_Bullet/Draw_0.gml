draw_circle_color(x, y, 5, c_red, c_red, false);
draw_line(x + lengthdir_x(baseSpeed, direction) * 3, y + lengthdir_y(baseSpeed, direction) * 3, x - lengthdir_x(baseSpeed, direction) * 3, y - lengthdir_y(baseSpeed, direction) * 3);